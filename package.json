{
  "name": "async-pocket-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "generate:data": "node scripts/generate-showdown-data.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "pokemon-showdown": "^0.11.10",
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.0.14"
  }
}
